services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: handover
      POSTGRES_PASSWORD: handover
      POSTGRES_DB: handover
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata:
