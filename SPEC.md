# HANDOVER — אפיון מוצר וטכני מלא
**גרסה:** 0.1 · **תאריך:** 26.8.2026 · **סטטוס:** אפיון לביצוע
**שם קוד:** HANDOVER (שם זמני — לא נבחר שם מסחרי)

---

## 0. תקציר מנהלים

### 0.1 מה אנחנו בונים
מערכת שמודדת, מאמתת ומעבירה עומסי עבודה של מודלי שפה בין ספקים.

שלושה מוצרים על גבי מנוע אחד:

| # | מוצר | קונה | תפקיד עסקי |
|---|------|------|-------------|
| 1 | **מנוע הגירה** (Handover) | CTO / VP Eng, ומעבדות AI | המוצר שמייצר הכנסה |
| 2 | **מדד ציבורי** (Index) | כולם, חינם | ערוץ הפצה ותשומת לב |
| 3 | **קבלה חודשית + ראיית ציות** (Receipt) | CFO, סיכון וציות | הכנסה חוזרת ונעילה |

### 0.2 התובנה המרכזית
היחידה הכלכלית אינה טוקן אלא **משימה שהושלמה בהצלחה מאומתת**.
מודל "זול לטוקן" ששורף טוקני חשיבה וריטריים הוא היקר בפועל. כל המערכת בנויה סביב היחידה הזו.

### 0.3 התובנה הטכנית
אין ידע בתוך המודל היוצא — מודלים חסרי מצב.
הידע יושב בפרומפטים, בסכמות הכלים ובהתנהגות בפועל, **וניתן לחלץ אותו מהעקבות** של התעבורה ההיסטורית.

### 0.4 מה אנחנו לא בונים
- שער / פרוקסי מתחרה (נכנסים כפלאגין לשערים קיימים)
- DLP, זיהוי הזרקות פרומפט, סיווג ג'יילברייק, גילוי AI צללים
- ניתוב בזמן אמת (ממליצים, לא מנתבים — שמירה על עצמאות)
- שירות ייעוץ ידני שלא משאיר קוד מאחוריו

**נימוק לשלילת האבטחה:** DLP דורש קריאת תוכן. כל הארכיטקטורה שלנו היא מטא-דאטה בלבד. שילוב השניים הורס את טיעון הפרטיות, מכניס אותנו לתקציב השהיה של <100ms, ומעמיד אותנו מול 30 ספקים ממומנים. אבטחה = חברה אחרת.

---

## 1. עקרונות ארכיטקטוניים מחייבים

עקרונות אלה גוברים על כל החלטת יישום. כל סטייה מהם דורשת עדכון מסמך זה.

1. **P1 — הפרדת תוכן.** תוכן בקשות ותשובות **לעולם לא יוצא מסביבת הלקוח**. יוצאים החוצה רק: מטא-דאטה, האשים, וציונים מצרפיים.
2. **P2 — אימות לפני שיפוט.** ציון נקבע קודם כל בבדיקה תוכניתית. שיפוט מודל הוא מוצא אחרון ומסומן ככזה.
3. **P3 — כל מדד נושא אי-ודאות.** אין מספר בלי גודל מדגם ורווח סמך. התרעה בלי מובהקות סטטיסטית = באג.
4. **P4 — דרגת ראיה.** כל שדה מסומן `measured` / `derived` / `declared`. אין ערבוב.
5. **P5 — אפס תלות בזמן אמת.** המערכת א-סינכרונית לחלוטין. תקלה אצלנו לא נוגעת בייצור של הלקוח.
6. **P6 — תקציב עלות מפורש.** לכל תהליך שקורא למודל יש תקרת הוצאה יומית בקונפיג. חריגה = עצירה, לא התראה.
7. **P7 — הכל מקומי.** אין פרסום ציבורי של קוד או נתוני לקוח ללא החלטה מפורשת. פלטים נשמרים בתיקיות העבודה.

---

## 2. ארכיטקטורה

### 2.1 תרשים רכיבים

```
[ סביבת הלקוח — in-tenant ]
  Collector Adapters ──> Trace Normalizer ──> Local Store (Postgres)
                                │
                                ├──> Task Assembler
                                ├──> Verification Engine
                                ├──> Shadow Replay Worker   (רואה תוכן)
                                ├──> Cluster Extractor
                                └──> Handover Pack Builder
                                          │
                                    Score Exporter  ── מטא-דאטה בלבד ──┐
                                                                        │
[ הענן שלנו ]                                                           v
  Aggregation Service ──> Index Service ──> Public Index (read-only)
                     └──> Canary Runner  ──> Drift Detector ──> Alerts
                     └──> Report Renderer ──> Monthly Receipt / Evidence Pack
```

### 2.2 רכיבים

| רכיב | אחריות | קריטיות |
|------|--------|---------|
| `collector` | קליטת עקבות ממקורות שונים, נרמול לסכמה אחת | P0 — הליבה |
| `normalizer` | המרה לסכמת Trace אחידה, האשינג, רדקציה | P0 |
| `task_assembler` | קיבוץ ניסיונות חוזרים למשימה אחת | P0 |
| `verifier` | קביעת הצלחה/כישלון לפי אות אובייקטיבי | P0 |
| `metrics` | CPAT, TTAT, ניתוח בזבוז | P0 |
| `reporter` | דוח HTML/Excel מקומי | P0 |
| `cli` | כלי חינמי למפתחים — מייצר את הפאנל | P1 |
| `clusterer` | חילוץ טקסונומיית משימות | P1 |
| `goldset` | בניית סט זהב ממקרים מאומתים | P1 |
| `packer` | הרכבת חבילת מסירה | P1 |
| `replayer` | הרצה מקבילה על מודלים חלופיים | P2 |
| `gap_report` | דוח פער בין מודל יוצא לנכנס | P2 |
| `prompt_translator` | התאמת פרומפטים למודל היעד | P2 |
| `canary` | דגימה מתוזמנת של כל המודלים | P3 |
| `drift` | זיהוי שינוי מובהק סטטיסטית | P3 |
| `index` | חישוב ופרסום המדד | P3 |
| `evidence` | ייצוא חבילת ראיות לביקורת | P3 |

---

## 3. מודל הנתונים

### 3.1 Trace — הרשומה האטומית

**אין תוכן ברשומה זו.** זו הרשומה שיוצאת החוצה (אחרי צבירה).

```json
{
  "trace_id": "uuid",
  "tenant_id": "uuid",
  "task_id": "uuid",
  "attempt_no": 1,

  "ts_start": "2026-08-26T14:03:22.114Z",
  "ts_end":   "2026-08-26T14:04:03.882Z",

  "provider": "anthropic",
  "model_id": "claude-sonnet-4-6",
  "model_version_hint": "2026-06-01",
  "endpoint_region": "us-east-1|unknown",

  "tokens": {
    "input": 8400,
    "input_cached": 6100,
    "output": 3100,
    "reasoning": 2600
  },
  "cost_usd": 0.0831,

  "latency": { "ttft_ms": 640, "total_ms": 41768 },

  "input_shape": {
    "n_messages": 12,
    "n_chars": 31402,
    "has_attachments": false,
    "system_prompt_fingerprint": "sha256:a91f...",
    "input_fingerprint": "sha256:77c2..."
  },

  "output_shape": {
    "type": "json|text|code|mixed",
    "n_chars": 4820,
    "json_valid": true,
    "schema_fingerprint": "sha256:12ab...",
    "has_code_block": true,
    "n_code_blocks": 2
  },

  "tool_calls": [
    { "name": "run_tests", "args_fingerprint": "sha256:...",
      "ts_offset_ms": 12400, "duration_ms": 8100, "status": "ok" }
  ],

  "verification": {
    "status": "pass|fail|unknown",
    "method": "programmatic|downstream|judge",
    "signal": "test_exit_code|schema_validation|silent_acceptance|jury",
    "confidence": 1.0,
    "evidence_grade": "measured|derived|declared"
  },

  "redaction_level": "metadata_only",
  "schema_version": "1.0"
}
```

**הערות יישום:**
- `system_prompt_fingerprint` — האש של תבנית הפרומפט **אחרי הסרת משתנים**. מאפשר לזהות שינוי פרומפט אצל הלקוח בלי לראות אותו.
- `input_fingerprint` — לדה-דופליקציה בשידור חוזר. חוסך כסף אמיתי.
- `reasoning` — קריטי. זה המשתנה שמסביר את רוב הפער בין מחיר נקוב לעלות אמיתית.

### 3.2 Task — יחידת החשבון

```json
{
  "task_id": "uuid",
  "tenant_id": "uuid",
  "cluster_id": "c07",
  "attempts": 2,
  "succeeded": true,
  "first_attempt_success": false,
  "total_cost_usd": 0.1662,
  "total_wall_ms": 82190,
  "total_tokens": { "input": 16800, "output": 6200, "reasoning": 5200 },
  "models_used": ["claude-sonnet-4-6"],
  "verification_grade": "measured"
}
```

**חוקי הרכבה (task_assembler):**
1. אם הלקוח מספק `task_id` — משתמשים בו. זו הדרך המועדפת.
2. אחרת היוריסטיקה: אותו `session_id` + אותו `system_prompt_fingerprint` + פער < 120 שניות + חפיפת קלט > 0.8 → אותה משימה.
3. משימה נחשבת שהצליחה אם **ניסיון אחד לפחות** אומת כהצלחה.
4. משימה בלי אות אימות → `verification_grade: unknown`, **ומוצאת מכל חישובי CPAT**. לא מנחשים.

### 3.3 טבלאות (Postgres)

```
tenants            (id, name, created_at, budget_daily_usd, retention_days)
traces             (partitioned by day, ts_start)
tasks
clusters           (id, tenant_id, label, centroid_ref, n_tasks)
golden_cases       (id, cluster_id, input_ref, expected_ref, verifier_spec)
model_registry     (provider, model_id, price_in, price_out, price_cached, first_seen)
canary_runs        (id, model_id, cluster_id, ts, n, pass_rate, ci_low, ci_high)
drift_events       (id, model_id, cluster_id, detected_at, delta, p_value, status)
handover_packs     (id, tenant_id, from_model, to_model, created_at, status)
gap_reports        (id, pack_id, cluster_id, delta_pass, delta_cost, delta_latency)
```

`input_ref` / `expected_ref` הם מצביעים לאחסון **בתוך סביבת הלקוח**. לעולם לא תוכן בטבלה המרכזית.

---

## 4. הגדרות מדדים — מדויקות

### 4.1 מדדי ליבה

```
success_rate      = successful_tasks / verified_tasks
CPAT              = Σ cost(all attempts) / successful_tasks        [USD]
TTAT              = Σ wall_time(all attempts) / successful_tasks    [sec]
attempts_per_win  = Σ attempts / successful_tasks
```

**חשוב:** המכנה הוא משימות שהצליחו, המונה הוא כל העלות כולל הכישלונות. זה מה שהופך את המדד לאמיתי.

### 4.2 פירוק בזבוז

```
waste_retry     = Σ cost(failed attempts of eventually-successful tasks)
waste_reasoning = Σ max(0, reasoning_tok - p50_reasoning_of_cluster_successes) × price_out
waste_context   = Σ (input_tok - input_cached_tok) × price_in
                    שניתן היה לשמור במטמון (זוהה prefix חוזר)
waste_dead      = Σ cost(tasks that never succeeded)
```

`waste_reasoning` הוא אומדן (`derived`), לא מדידה. חובה לסמן ככזה בדוח.

### 4.3 מדד ההשוואה (שלושה צירים + משקולת משתמש)

```
Quality  = success_rate                          [0..1]
Speed    = 1 / TTAT           (מנורמל לוג)
Value    = 1 / CPAT           (מנורמל לוג)

Score(w) = w_q·norm(Quality) + w_s·norm(Speed) + w_v·norm(Value)
```

- ברירת מחדל: `w = (0.6, 0.2, 0.2)`
- המשקולות נחשפות למשתמש כמחוונים.
- **בחירות המשקולות נאספות כטלמטריה מצרפית** — זהו נכס נתונים בפני עצמו (מה השוק באמת מתמחר).
- נרמול: min-max על פני כל המודלים בתוך אותו אשכול משימות. **לעולם לא בין אשכולות.**

---

## 5. מנוע האימות

### 5.1 היררכיית אותות — לפי חוזק ראייתי

| דרגה | שיטה | דוגמאות | ראיה |
|------|------|---------|------|
| A | תוכניתית | קוד יציאה של טסט, ולידציית סכמה, קומפילציה, השוואה לאמת ידועה, בדיקת שפיות מספרית | `measured` |
| B | קבלה במורד הזרם | הדיף אושר, לא היה ריטריי תוך 10 דקות, לא הייתה הודעת תיקון | `derived` |
| C | חבר מושבעים | 3 מודלים משלוש מעבדות + רובריקה | `derived` |
| — | אין אות | `unknown` — יוצא מכל חישוב | — |

**יעד תמהיל:** ≥70% דרגה A, ≤20% דרגה C.
לקוח שאי אפשר להגיע אצלו ל-50% דרגה A **אינו לקוח מתאים ל-P0/P1**. זה קריטריון סינון מכירתי, לא רק טכני.

### 5.2 כללי חבר המושבעים (דרגה C)

```
- 3 שופטים ממעבדות שונות
- מודל לעולם לא שופט פלט של משפחתו   ← חובה קשיחה
- כל השוואה זוגית רצה פעמיים בסדר הפוך  (נטרול הטיית מיקום)
- רובריקה עם ניכוי מפורש על מלל מיותר   (נטרול הטיית אורך)
- מדווחים תמיד: Fleiss' kappa בין השופטים
- kappa < 0.4  →  התוצאה מסומנת low_agreement ולא נכנסת לציון פומבי
- סט זהב אנושי: 200 פריטים, כיול מחדש רבעוני
```

### 5.3 ממשק Verifier (הרחבה על ידי הלקוח)

```python
class Verifier(Protocol):
    name: str
    grade: Literal["measured", "derived"]
    def applies_to(self, task: Task) -> bool: ...
    def verify(self, task: Task, artifacts: Artifacts) -> Verdict: ...
```

מימושים ב-P0: `PytestVerifier`, `JsonSchemaVerifier`, `ExitCodeVerifier`, `RegexContractVerifier`, `SilentAcceptanceVerifier`.

---

## 6. זיהוי סחף — הסטטיסטיקה

זה הרכיב שהכי קל לעשות בו טעות ולהרוס בו את אמינות המוצר.

### 6.1 גודל מדגם

לזיהוי ירידה של Δ נקודות אחוז משיעור בסיס p, ברמת מובהקות 0.05 ועוצמה 0.8:

```
n ≈ (z_{α/2}·√(2p̄q̄) + z_β·√(p₁q₁ + p₂q₂))² / Δ²
```

מספרים מעשיים (p = 0.90):

| Δ שרוצים לזהות | n לכל צד |
|---|---|
| 15 נק' | ~70 |
| 10 נק' | ~150 |
| 5 נק' | ~570 |
| 2 נק' | ~3,400 |

**מסקנה תפעולית:** קנארי שעתי (n≈30) מזהה רק קריסות. אסור להציג ממנו הכרזות.

### 6.2 קצבי הרצה

| מחזור | n לכל מודל | שימוש | תווית ב-UI |
|---|---|---|---|
| שעתי | 30 | דופק, זיהוי קריסה >15 נק' | "pulse" |
| יומי | 600 | זיהוי 5 נק' | "daily" |
| שבועי | 5,000 | פירוק לפי אשכול, הכרזות פומביות | "confirmed" |

### 6.3 חוקי התרעה

```
alert = TRUE  אם ורק אם:
   (1) רווח סמך Wilson של הדלתא אינו כולל אפס
   (2) |delta| > min_effect (ברירת מחדל 5 נק')
   (3) התנאי מתקיים בשני חלונות רצופים  אוֹ  CUSUM חצה סף
   (4) אחרי תיקון Benjamini-Hochberg על פני כל צמדי model×cluster
```

בלי סעיף (4) — עם 40 מודלים × 12 אשכולות = 480 השוואות יומיות, נקבל ~24 התרעות שווא ביום ברמת 0.05. זה הורג את המוצר בשבוע.

### 6.4 מה מפרסמים בהתרעה

```
Model X · cluster "structured extraction"
pass rate: 0.91 → 0.78   (Δ = -13.0 נק', CI95: [-17.2, -8.4])
n = 610 · confirmed over 2 windows · FDR-adjusted q = 0.003
first observed: 2026-08-26 06:00Z
```

---

## 7. מנוע ההגירה — הלב

### 7.1 חבילת המסירה (Handover Pack)

```
handover-pack/
├── manifest.json          מטא-דאטה, גרסאות, חתימה
├── taxonomy.json          אשכולות: label, n_tasks, share_of_cost
├── contract.json          חוזה פלט משתמע לכל אשכול
├── tool_policy.json       גרף קריאות כלים, תדירויות, אילוצי סדר
├── edge_cases.jsonl       סטיות מהדפוס, מדורגות לפי תדירות × עלות
├── prompts/               פרומפטים מחולצים + חריצי משתנים
├── golden/                מקרי זהב מאומתים לכל אשכול
└── baseline.json          מדדי המודל היוצא לכל אשכול
```

### 7.2 שלבי החילוץ

**שלב 1 — אשכול.**
הטמעה של `system_prompt_fingerprint` + `output_shape` + חתימת רצף הכלים → HDBSCAN.
יעד: 8–15 אשכולות. יותר מ-20 = פירוק יתר, מיזוג.
כל אשכול מקבל תווית קריאה שנוצרת על ידי מודל מ-5 מקרים מייצגים.

**שלב 2 — חוזה פלט.**
לכל אשכול: הסקת סכמת JSON מהפלטים המוצלחים (union עם דיווח שכיחות שדות), התפלגות אורך, מבנה חוזר.

**שלב 3 — מדיניות כלים.**
גרף מעברים בין כלים מהמקרים המוצלחים. חילוץ אילוצים: "כלי B אף פעם לא נקרא לפני A" (מבוסס תדירות, עם רמת ביטחון).

**שלב 4 — מקרי קצה.**
מקרים שסטו מהחוזה או מהמדיניות ובכל זאת הצליחו. אלה בדיוק המקרים שנשברים במעבר.

**שלב 5 — סט זהב.**
`n = max(30, 3% of cluster)` מקרים מאומתים בדרגה A לכל אשכול. שכבתי לפי עלות ולפי אורך.
**רק דרגה A נכנסת לסט הזהב.** אין פשרות כאן.

### 7.3 תהליך המעבר

```
1. EXTRACT   חבילת מסירה מהתעבורה ההיסטורית          (אוטומטי)
2. TRANSLATE התאמת פרומפטים לניבי מודל היעד           (אוטומטי + סקירה)
3. MATCH     ריצת סט הזהב על שני המודלים               (אוטומטי)
4. GAP       דוח פער לכל אשכול                        ← כאן הערך
5. FIX       אופטימיזציית פרומפט על אשכולות כושלים בלבד (לולאה חסומה)
6. ROLLOUT   shadow → 5% → 25% → 100%, חזרה אוטומטית
```

**שלב 5 — לולאה חסומה חובה:** מקסימום 8 איטרציות לאשכול, תקציב מפורש, עצירה כשהשיפור < 1 נק' בשתי איטרציות רצופות. בלי החסימות האלה זה שורף אלפי דולרים בלילה.

### 7.4 פורמט דוח הפער

```
cluster              share_cost  from    to      Δpass   Δcost   Δlat   verdict
────────────────────────────────────────────────────────────────────────────────
structured_extract      31%      0.94   0.96    +2.0    -71%    -38%   PASS
code_fix                24%      0.91   0.89    -2.0    -64%    -12%   PASS
long_agentic            18%      0.88   0.71   -17.0    -58%    +21%   BLOCK
summarize               11%      0.96   0.97    +1.0    -70%    -44%   PASS
...
────────────────────────────────────────────────────────────────────────────────
11/12 אשכולות עברו. חסימה אחת: long_agentic.
עבודה נדרשת: אשכול אחד. הערכה: 3–5 ימי מהנדס.
```

זו השורה שמוכרת את המוצר. הכל קיים כדי לייצר אותה.

### 7.5 מה לא ניתן להעביר — ואסור להבטיח

- **מודל מכוונן (fine-tuned)** — דורש אימון מחדש. מציינים במפורש בהצעה.
- **הטמעות וקטוריות** — בדרך כלל מודל נפרד; לא נוגעים. אם כן משנים — נדרש אינדוקס מחדש מלא. עלות נפרדת.
- **לקוח בלי אות אימות** — נופלים לדרגה C, הדיוק צונח, והמוצר לא עומד בהבטחה.

---

## 8. פרטיות ואבטחת מידע

| נתון | היכן נמצא | יוצא החוצה |
|---|---|---|
| תוכן בקשה/תשובה | סביבת הלקוח בלבד | לעולם לא |
| סט זהב | סביבת הלקוח בלבד | לעולם לא |
| האשים ו-fingerprints | שניהם | כן (חד-כיווני, מלוח) |
| ספירות טוקנים, עלות, זמנים | שניהם | כן |
| ציוני הצלחה מצרפיים | שניהם | כן |
| תוויות אשכולות | שניהם | כן, לאחר אישור לקוח |

- כל האשינג: SHA-256 עם מלח לכל דייר. מלח לא יוצא.
- שמירת עקבות גולמיות: ברירת מחדל 30 יום, ניתן לקונפיגורציה.
- שידור חוזר רץ **אך ורק בסביבת הלקוח**, כי הוא זקוק לתוכן.
- מצב `air-gapped` נתמך: ייצוא ידני של קובץ ציונים בלבד.

---

## 9. מפת דרכים — שלבים וקריטריוני קבלה

### P0 · שבועות 1–2 · הליבה
**מטרה:** מעקבות גולמיות לדוח CPAT.

- סכמת Trace + נרמול
- אדפטר ראשון: LiteLLM callback
- אדפטר שני: קריאת קובץ JSONL
- מרכיב משימות
- שני מאמתים: `ExitCode`, `JsonSchema`
- חישוב CPAT / TTAT / פירוק בזבוז
- דוח HTML מקומי

**קבלה:** קליטת 10,000 עקבות סינתטיות + סט אמיתי אחד → דוח שמציג CPAT לכל מודל, פירוק בזבוז, ואחוז `unknown` מתחת ל-30%.

### P1 · שבועות 3–4 · הפאנל וחבילת המסירה
- CLI חינמי: `hv record`, `hv report`, `hv pack`
- אשכול (HDBSCAN) + תיוג אשכולות
- חילוץ חוזה פלט ומדיניות כלים
- בונה סט זהב
- ייצוא חבילת מסירה

**קבלה:** מקובץ עקבות אמיתי אחד נוצרת חבילת מסירה תקינה עם 8–15 אשכולות ו-≥30 מקרי זהב לאשכול הגדול.

### P2 · שבועות 5–8 · ההגירה
- שידור חוזר בסביבת הלקוח, עם תקציב
- מתרגם פרומפטים
- דוח פער
- לולאת תיקון חסומה
- תוכנית פריסה הדרגתית + חזרה אוטומטית

**קבלה:** הגירה מקצה לקצה על עומס פיילוט אחד, עם דוח פער מדיד ותוצאה: לפחות 70% מהאשכולות עוברים ללא התערבות.

### P3 · שבועות 9–12 · המדד והראיה
- שירות קנארי מתוזמן
- מזהה סחף עם כל ארבעת חוקי ההתרעה
- שירות מדד + עמוד ציבורי (סטטי)
- ייצוא חבילת ראיות לביקורת
- קבלה חודשית

**קבלה:** 30 יום רצופים של סדרה עתית ללא פערים, ואירוע סחף אמיתי אחד לפחות שזוהה ואומת ידנית.

**קריטריון עצירה (חובה להגדיר מראש):** אם ב-60 יום מזוהה פחות מאירוע סחף אמיתי אחד לחודש — מוצר ההתרעות נסגר ונשארים עם ההגירה והקבלה בלבד.

---

## 10. מחסנית טכנולוגית

| שכבה | בחירה | נימוק |
|---|---|---|
| שפה | Python 3.12 | אקוסיסטם הערכה ונתונים |
| API | FastAPI + Pydantic v2 | ולידציה חזקה, מהיר לכתיבה |
| DB | Postgres 16 (partitioned) | מספיק לחלוטין עד מיליארדי שורות |
| אנליטיקה | DuckDB + Parquet | צבירה מקומית בלי אשכול נתונים |
| תורים | Redis + arq | פשוט, מספיק |
| CLI | Typer | |
| דוחות | Jinja2 → HTML (+ WeasyPrint ל-PDF) | ללא תלות ענן |
| אשכול | scikit-learn + HDBSCAN | |
| פריסה | docker-compose על VPS יחיד | **לא Kubernetes.** לא בשנה הראשונה |
| בדיקות | pytest + fixtures מוקלטים | אפס קריאות API אמיתיות בטסטים |

**החלטות שנדחות בכוונה:** אין vector DB (Postgres + pgvector מספיק), אין k8s, אין microservices, אין frontend framework ב-P0 (HTML סטטי מיוצר).

---

## 11. חיסכון בעלויות — תכנון וביצוע

### 11.1 חיסכון בעלות הריצה של המוצר

| טכניקה | חיסכון | הערה |
|---|---|---|
| Batch API לכל שידור חוזר ושיפוט | ~50% | הכל אצלנו א-סינכרוני ממילא |
| Prompt caching לפרומפטי שופט וקנארי | עד 90% על הקידומת | הפרומפטים קבועים — התאמה מושלמת |
| דה-דופליקציה לפי `input_fingerprint` | 10–30% | קלטים חוזרים נפוצים מהצפוי |
| דגימה שכבתית לפי עלות, לא אחידה | 60–80% | דוגמים יותר מאשכולות יקרים |
| קנארי קטן יומי + מלא שבועי | ~85% מול מלא יומי | ראה §6.2 |
| מודל קטן כשופט ראשון, הסלמה במחלוקת | 70–85% | רק ~15% מגיעים לשופט החזק |
| `max_tokens` הדוק לכל תפקיד | 10–20% | שופט לא צריך 4096 |
| כיבוי חשיבה היכן שאינה נדרשת | 30–60% | המשתנה הגדול ביותר |
| תפוגת עקבות גולמיות ב-30 יום | אחסון | צבירות נשמרות לנצח |
| תקרת תקציב יומית קשיחה לכל דייר | הגנה | P6 |

**אומדן עלות תפעולית חודשית ב-P0–P2:** VPS ~$40, אחסון ~$10, קריאות מודל לפיתוח ובדיקות $150–400. סה"כ מתחת ל-$500 לחודש.
**ב-P3 עם קנארי מלא:** $600–2,000 לחודש, תלוי במספר המודלים ובגודל המדגם. זו ההוצאה התפעולית העיקרית ויש לנטר אותה כמו שכר.

### 11.2 חיסכון בעבודה מול Claude Code

| כלל | למה |
|---|---|
| **מסמך אפיון לפני קוד** | זה המסמך. הגורם היחיד עם ההשפעה הגדולה ביותר על העלות |
| `CLAUDE.md` בשורש הריפו | חוסך הסבר חוזר של מחסנית ומוסכמות בכל סשן |
| משימה אחת = קובץ אחד = טסט אחד | סשן קצר, הקשר קטן, עלות נמוכה |
| לבקש תוכנית לפני קוד, לאשר, ואז לבצע | מונע כתיבה מחדש של מודולים שלמים |
| `/clear` בין משימות | ההקשר המצטבר הוא ההוצאה הגדולה ביותר |
| להצביע על קבצים ספציפיים, לא "תקרא את הריפו" | קריאת ריפו שלם = עשרות אלפי טוקנים לכל סשן |
| טסט כושל קודם, ואז מימוש | מגדיר קבלה חד-משמעית, מונע סבבים |
| מודל חזק לתכנון וללוגיקה מסובכת, מודל זול לפיגומים | חלוקה של ~30/70 |
| קבצים מתחת ל-300 שורות | עריכה נקודתית במקום שכתוב |
| fixtures מוקלטים בטסטים | אפס עלות API בכל ריצת טסטים |
| לא לבנות UI ב-P0 | HTML מיוצר. UI הוא בור הזמן הגדול ביותר |

---

## 12. מבנה הריפו

```
handover/
├── CLAUDE.md
├── SPEC.md                  ← המסמך הזה
├── TASKS.md
├── pyproject.toml
├── docker-compose.yml
├── src/handover/
│   ├── schema/              trace.py, task.py, pack.py
│   ├── collect/             litellm_adapter.py, jsonl_adapter.py, normalizer.py
│   ├── assemble/            task_assembler.py
│   ├── verify/              base.py, exit_code.py, json_schema.py, jury.py
│   ├── metrics/             core.py, waste.py, index.py
│   ├── cluster/             extractor.py, labeler.py
│   ├── pack/                builder.py, contract.py, tool_policy.py, goldset.py
│   ├── replay/              runner.py, budget.py
│   ├── migrate/             translator.py, gap_report.py, optimizer.py
│   ├── canary/              scheduler.py, drift.py, stats.py
│   ├── report/              renderer.py, templates/
│   └── cli/                 main.py
└── tests/
    ├── fixtures/            traces_sample.jsonl, golden_pack/
    └── ...
```

---

## 13. סיכונים

| סיכון | חומרה | הפחתה |
|---|---|---|
| אחוז `unknown` גבוה אצל לקוחות אמיתיים | **קריטי** | סינון לקוחות לפי יכולת אימות. זה קריטריון מכירה |
| התרעות שווא הורגות אמון | גבוה | §6.3, ארבעת התנאים. אין קיצורי דרך |
| הקטגוריה נבלעת בפלטפורמות | גבוה | להגיע לתקן ולחוזים מהר; לא להישאר בשכבת הכלי |
| ספקים חוסמים שידור חוזר | בינוני | הכל רץ תחת מפתחות הלקוח, בקצב רגיל |
| ניגוד עניינים אם מעבדות משלמות | **קריטי** | §14 |
| עלות קנארי מתפוצצת | בינוני | P6, תקציב קשיח |
| הגירה = הכנסה חד-פעמית | גבוה | מנוי "כשירות ניידות" + השבתות מודלים כמחזור חוזר |

---

## 14. מדיניות ניגוד עניינים — לא ניתנת לפשרה

1. **דירוג לעולם לא נמכר.** אף מעבדה, בשום תנאי, לא משלמת עבור מיקום, סדר, או הצגה במדד.
2. מעבדה יכולה לשלם על: פיד נתונים תחרותי, ומימון הגירה של לקוח **שכבר החליט לעבור**.
3. כל מימון מעבדה מגולה בגוף הדוח שהלקוח מקבל.
4. המתודולוגיה פומבית וגרסתית. שינוי מתודולוגיה מפורסם 30 יום מראש.
5. סדרות עתיות אינן נערכות רטרואקטיבית. תיקון = רשומת תיקון חדשה.

בלי חמישת אלה, המדד חסר ערך ותוך שנתיים גם חסר מוניטין.

---

## 15. החלטות פתוחות — דורשות הכרעה לפני P1

1. **שם ודומיין.**
2. **עומס הפיילוט הראשון** — צריך לקוח או פרויקט פנימי עם אימות תוכניתי חזק.
3. **קוד פתוח ל-CLI?** המלצה: כן, Apache-2.0 לאספן בלבד. המנוע סגור. זה מה שמייצר את הפאנל.
4. **ישות משפטית ומקום התאגדות** — רלוונטי למכירה לארגונים באירופה.
5. **האם מקבלים מימון מעבדות בשלב זה** — המלצה: לא לפני שיש 5 הגירות בתשלום מלקוחות.
6. **תאריכי EU AI Act** — נדרש אימות משפטי לפני שימוש שיווקי. המקורות סותרים לגבי מערכות בסיכון גבוה.
